const articleSchema = require('./article-schema');
const loginSchema = require('./login-schema');
const recruitmentSchema = require('./recruitment-schema');
const teamSchema = require('./team-schema');

module.exports = {
  articleSchema, loginSchema, recruitmentSchema, teamSchema,
};
