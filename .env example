PORT=3000
# if http MODE
# DEV=anything
# if https MODE
# PROD=anything
PGDATABASE=victoryzone
PGHOST=localhost
PGPORT=5432
PGUSER=victoryzone
PGPASSWORD=victoryzone
JWT_SECRET=oclock
JWT_REFRESH_SECRET=oclock
ACCESS_TOKEN_EXPIRATION=15m
REFRESH_TOKEN_EXPIRATION=7d
